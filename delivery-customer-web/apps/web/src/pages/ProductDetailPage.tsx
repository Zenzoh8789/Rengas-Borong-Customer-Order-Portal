import { ArrowLeft,Star } from "lucide-react"; import { useEffect,useMemo,useState } from "react"; import { useNavigate,useParams } from "react-router-dom"; import { BrandLogo } from "../components/BrandLogo"; import { ProductCard } from "../components/ProductCard"; import { useApp } from "../context/AppContext"; import { api } from "../services/api"; import type { Product,ProductCategory } from "../types";
const categoryName=(c:ProductCategory)=>typeof c==="string"?c:c?.name||"Uncategorised";
export function ProductDetailPage(){const{productId}=useParams();const navigate=useNavigate();const{cart,setQuantity,notify}=useApp();const[products,setProducts]=useState<Product[]>([]);const[expanded,setExpanded]=useState(false);const[quantity,setLocalQuantity]=useState(1);useEffect(()=>{api.products().then(setProducts).catch(()=>setProducts([]));},[]);const product=products.find(p=>p.id===Number(productId));const uom=product?.uoms[0];const related=useMemo(()=>product?products.filter(p=>p.id!==product.id&&categoryName(p.category)===categoryName(product.category)).slice(0,6):[],[product,products]);if(!product)return <div className="detail-loading">Loading product…</div>;const description=product.description||product.subtitle||`${product.name} is available for wholesale ordering. Quality stock selected for your business and delivered with care.`;const add=()=>{if(!uom)return;const current=cart.find(x=>x.product.id===product.id&&x.uom.id===uom.id)?.quantity||0;setQuantity(product,uom,current+quantity);notify(`${quantity} × ${product.name} added to cart.`,"success");};return <div className="product-detail-page"><button className="detail-back" onClick={()=>navigate(-1)}><ArrowLeft/> Product details</button><div className="detail-scroll"><div className="detail-image"><BrandLogo size={260} src={product.imageUrl} alt={product.name}/></div><div className="detail-info"><small>{categoryName(product.category)}</small><h1>{product.name}</h1>{product.rating!=null&&<div className="detail-rating"><Star fill="currentColor"/> {product.rating.toFixed(1)}</div>}<div className="detail-price">RM {Number(uom?.price||0).toFixed(2)} <span>/ {uom?.pack||uom?.name||"unit"}</span></div><div className={`detail-description ${expanded?"expanded":""}`}><h3>Description</h3><p>{description}</p>{description.length>90&&<button onClick={()=>setExpanded(!expanded)}>{expanded?"Show less":"Read more"}</button>}</div></div>{related.length>0&&<section className="more-products"><header><h2>More Products</h2><button onClick={()=>navigate(`/?category=${encodeURIComponent(categoryName(product.category))}`)}>See more</button></header><div className="product-row">{related.map(item=><ProductCard product={item} key={item.id}/>)}</div></section>}</div><div className="detail-cart-bar"><div className="detail-stepper"><button onClick={()=>setLocalQuantity(Math.max(1,quantity-1))}>−</button><b>{quantity}</b><button onClick={()=>setLocalQuantity(quantity+1)}>+</button></div><button className="detail-add" disabled={!uom} onClick={add}>Add to Cart · RM {(Number(uom?.price||0)*quantity).toFixed(2)}</button></div></div>}
