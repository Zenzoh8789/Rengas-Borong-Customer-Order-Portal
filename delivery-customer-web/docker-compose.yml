services:
  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
      args:
        VITE_API_URL: "http://localhost:3002/api"

    container_name: rengas-customer-web
    restart: unless-stopped

    ports:
      - "5173:80"